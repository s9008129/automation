# RPA-Cowork — .env 範例
# 請把這個檔案複製成 .env，再只修改你需要的欄位
# ⚠️ .env 只供本機使用，不可提交到 Git，也不應被打包進離線工具包

# 錄製檔中的密碼欄位會改用這個值
CERT_PASSWORD=你的實際憑證密碼

# ─── 🔐 加密說明 ───────────────────────────────────────────
# 填好上方的值後，可執行 .\protect-env.ps1 加密所有敏感值。
# 加密後的值會變成 ENC(...) 格式。
#
# 🔄 run-task.ps1 會自動處理：
#   執行前 → 自動解密 .env
#   執行後 → 自動更換金鑰 + 重新加密
#   使用者不需要手動解密或加密。
#
# 常用指令：
#   .\protect-env.ps1             加密（首次會自動產生 .env.key 金鑰）
#   .\protect-env.ps1 -Status     查看加密狀態
#   .\protect-env.ps1 -Decrypt    解密（還原明文以便修改）
#   .\protect-env.ps1 -RotateKey  換鑰（產生新金鑰並重新加密）
#
# ⚠️ .env.key 是解密的唯一金鑰，請妥善保管，不可提交到 Git。