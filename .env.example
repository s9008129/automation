# Environment variables for recording credentials
# Copy this file to .env and fill in your actual credentials

# Generic recording credentials (recommended)
RECORDING_USERNAME=your-username-here
RECORDING_PASSWORD=your-password-here

# Domain-specific credentials (optional - use if different domains need different creds)
# NCERT_USERNAME=ncert-username
# NCERT_PASSWORD=ncert-password

# Usage in recordings:
# await page.getByRole('textbox', { name: '帳號' }).fill(process.env.RECORDING_USERNAME);
# await page.getByRole('textbox', { name: '密碼' }).fill(process.env.RECORDING_PASSWORD);

# SECURITY WARNING:
# - Never commit the .env file with actual credentials
# - .env is in .gitignore by default
# - Recording files are automatically sanitized to use these env var names
